services:
  web:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "5001:5001"
    volumes:
      - ../data:/app/data
    environment:
      - FLASK_ENV=production
      - CATCHMENT_ID=thames
    restart: unless-stopped
